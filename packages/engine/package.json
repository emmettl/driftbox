{
  "name": "@driftbox/engine",
  "version": "0.5.0",
  "description": "A TR-808, a TR-909 and two TB-303s, synthesised from scratch with Web Audio. No recorded samples.",
  "keywords": [
    "web-audio",
    "drum-machine",
    "synthesis",
    "sequencer",
    "tr-808",
    "tr-909",
    "tb-303",
    "ladder-filter",
    "audioworklet"
  ],
  "license": "MIT",
  "author": "Louis Emmett",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emmettl/driftbox.git",
    "directory": "packages/engine"
  },
  "homepage": "https://emmettl.github.io/driftbox/",
  "type": "module",
  "//publishConfig": "Scoped packages are RESTRICTED by default, and restricted publishing needs a paid npm plan — so without this the first release fails with a 402 that no dry run catches, because --dry-run never makes the access check. Set here rather than passed as --access public on the command line so that publishing by hand from a laptop cannot differ from publishing from CI.",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "//files": "src ships alongside dist. The declaration maps and source maps in dist point at ../src/*.ts, so without it they dangle and 'go to definition' lands nowhere. It is also the more useful half of this package to have on hand: the reasoning in this engine lives in its comments, and a consumer who wants to know why the trim is applied after the waveshaper should be able to read that rather than infer it from minified output.",
  "files": [
    "dist",
    "src",
    "!src/**/*.test.ts",
    "README.md"
  ],
  "//engines": "A floor, not a support statement: nothing in either package uses an API newer than this. The engine compiles no further than ??= (ES2021), and the app’s bin uses only node: specifiers, ?? and optional chaining. CI builds and tests on 24.",
  "engines": {
    "node": ">=18"
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
