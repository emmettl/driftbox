// Compatibility entry point for rack-local imports and tests. The allocator and browser
// host are shared with the original groovebox editor; rack mode only adds polyphony and
// channel-aware module routing on top.
export * from '../midi.js'
